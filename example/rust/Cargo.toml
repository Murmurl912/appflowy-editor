[package]
name = "mermaid_ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib", "cdylib"]
name = "mermaid_ffi"

[dependencies]
mermaid-rs-renderer = { path = "../mermaid-rs-renderer", default-features = false }
